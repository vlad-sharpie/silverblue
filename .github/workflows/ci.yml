name: Create and publish a Docker image

on:
  schedule:
    - cron: "0 0 * * *"
  push:
    branches:
      - main

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: vlad-sharpie/silverblue
  IMAGE_NAME_NVIDIA: vlad-sharpie/silverblue-nvidia

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Buildah Action
        id: build-image
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.IMAGE_NAME }}
          tags: latest ${{ github.sha }}
          containerfiles: |
            ./Containerfile

      - name: Buildah Action NVIDIA
        id: build-image-nvidia
        uses: redhat-actions/buildah-build@v2
        with:
          image: ${{ env.IMAGE_NAME_NVIDIA }}
          tags: latest ${{ github.sha }}
          containerfiles: |
            ./ContainerfileNvidia

      - name: Push To GitHub Registry
        id: push-to-gh
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image.outputs.image }}
          tags: ${{ steps.build-image.outputs.tags }}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push To GitHub Registry NVIDIA
        id: push-to-gh-nvidia
        uses: redhat-actions/push-to-registry@v2
        with:
          image: ${{ steps.build-image-nvidia.outputs.image }}
          tags: ${{ steps.build-image-nvidia.outputs.tags }}
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}